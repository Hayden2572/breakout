# Breakout Game - Документация

## Описание проекта

**Breakout Game** - это классическая аркадная игра, разработанная на Python с использованием pygame. Игра включает поддержку множественных уровней сложности, системы сохранения статистики, и командной строки для кастомизации игровых параметров.

## Структура проекта

```
breakout-game/
├── main.py              # Главный модуль игры с классами Game, Paddle, Ball и т.д.
├── config.py            # Конфигурация и константы игры
├── stats_manager.py     # Менеджер статистики и сохранения результатов
├── test_game.py         # Модульные тесты
└── README.md            # Эта документация
```

## Установка и запуск

### Требования

- Python 3.7+
- pygame
- pytest (опционально для запуска тестов)

### Установка зависимостей

```bash
pip install pygame pytest
```

### Запуск игры

#### Базовый запуск (стандартные параметры)

```bash
python main.py
```

#### С пользовательским именем игрока

```bash
python main.py --name "YourName"
# или сокращенная форма
python main.py -n "YourName"
```

#### Выбор уровня сложности

```bash
python main.py --difficulty easy
python main.py --difficulty medium
python main.py --difficulty hard

# Сокращенная форма
python main.py -d hard
```

Уровни сложности:
- **easy**: Повышенное количество жизней (5), медленнее шар, движение платформы
- **medium** (по умолчанию): Стандартные параметры (3 жизни)
- **hard**: 1 жизнь, быстрый шар, сложное управление платформой

#### Установка максимального количества уровней

```bash
python main.py --levels 3
# или
python main.py -l 3
```

#### Примеры комбинации параметров

```bash
# Жесткая игра для опытного игрока с 10 уровнями
python main.py -n "ProPlayer" -d hard -l 10

# Легкая игра для новичка
python main.py -n "Beginner" -d easy
```

## Работа со статистикой

### Просмотр общей статистики

```bash
python main.py --show-stats
```

Выводит:
- Всего сыграно игр
- Количество побед и поражений
- Процент побед
- Средний, минимальный и максимальный счет
- Среднее время игры
- Количество уникальных игроков

### Просмотр статистики конкретного игрока

```bash
python main.py --show-player-stats "PlayerName"
```

### Просмотр топ рекордов

```bash
python main.py --show-top 10
```

### Очистка всей статистики

```bash
python main.py --clear-stats
```

## Управление игрой

### Клавиши управления

| Клавиша | Функция |
|---------|---------|
| **Стрелка ←** | Движение платформы влево |
| **Стрелка →** | Движение платформы вправо |
| **SPACE** | Запуск шара / Переход между экранами |
| **ESC** | Пауза / Возврат в меню |
| **P** | Пауза (во время игры) |

### Состояния игры

1. **MENU** - Главное меню
2. **PLAYING** - Активная игра
3. **PAUSED** - Пауза
4. **LEVEL_COMPLETE** - Уровень пройден
5. **GAME_OVER** - Проигрыш
6. **WIN** - Победа (все уровни пройдены)

## Архитектура кода

### Основные классы

#### `Game`
Главный класс управления игрой.

```python
game = Game(player_name="Player1", difficulty="medium", max_levels=5)
game.run()
```

**Ключевые методы:**
- `handle_events()` - Обработка входных данных
- `update()` - Обновление логики игры
- `render()` - Отрисовка экрана
- `_save_result()` - Сохранение результата игры

#### `Paddle`
Класс платформы.

```python
paddle = Paddle(speed_multiplier=1.0)
paddle.move_left()
paddle.move_right()
```

#### `Ball`
Класс шара с физикой столкновений.

```python
ball = Ball(x=400, y=300, speed_multiplier=1.0)
ball.launch()
ball.check_paddle_collision(paddle_rect)
ball.check_brick_collision(brick_rect)
```

#### `Brick` и `BrickGroup`
Классы кирпичей.

```python
brick_group = BrickGroup()
brick_group.generate_level(rows=6, cols=10)
```

#### `Level`
Управление уровнями игры.

```python
level = Level(level_number=1, difficulty="medium")
level.generate()
level.is_complete()
```

#### `StatsManager`
Менеджер статистики.

```python
stats = StatsManager()
stats.save_game_result("Player1", 1500, 3, "medium", 120.5, False)
stats.get_high_scores(10)
stats.get_statistics_summary()
```

## Формат сохранения статистики

Статистика сохраняется в JSON формате в директории `stats/`:

```json
[
  {
    "timestamp": "2024-01-15T10:30:45.123456",
    "player_name": "Player1",
    "score": 1500,
    "level_reached": 3,
    "difficulty": "medium",
    "game_duration": 120.5,
    "won": false
  },
  ...
]
```

## Тестирование

### Запуск всех тестов

```bash
python test_game.py
```

### Запуск тестов с pytest

```bash
pytest test_game.py -v
```

### Тестовые классы

- `TestStatsManager` - Тесты менеджера статистики
- `TestPaddle` - Тесты платформы
- `TestBrick` - Тесты кирпичей
- `TestBrickGroup` - Тесты группы кирпичей
- `TestBall` - Тесты шара
- `TestLevel` - Тесты управления уровнями
- `TestArgumentParser` - Тесты парсера аргументов командной строки

### Примеры тестов

```python
def test_save_game_result():
    stats = StatsManager()
    result = stats.save_game_result(
        player_name="TestPlayer",
        score=1500,
        level_reached=3,
        difficulty="medium",
        game_duration=120.5,
        win=False
    )
    assert result is True

def test_paddle_movement():
    paddle = Paddle()
    initial_x = paddle.x
    paddle.move_left()
    assert paddle.x < initial_x
```

## Параметры конфигурации

Все параметры игры находятся в `config.py`:

### Размеры и производительность

```python
WINDOW_WIDTH = 800          # Ширина окна
WINDOW_HEIGHT = 600         # Высота окна
FPS = 60                    # Кадры в секунду
```

### Параметры платформы

```python
PADDLE_WIDTH = 100          # Ширина платформы
PADDLE_HEIGHT = 15          # Высота платформы
PADDLE_SPEED = 8            # Базовая скорость
PADDLE_Y = WINDOW_HEIGHT - 30  # Y позиция
```

### Параметры шара

```python
BALL_SIZE = 8               # Радиус шара
BALL_SPEED = 5              # Базовая скорость
BALL_MAX_SPEED = 10         # Максимальная скорость
```

### Параметры кирпичей

```python
BRICK_WIDTH = 70            # Ширина кирпича
BRICK_HEIGHT = 15           # Высота кирпича
BRICK_ROWS = 6              # Начальное количество рядов
BRICK_COLS = 10             # Начальное количество колонок
BRICK_SPACING = 5           # Расстояние между кирпичами
```

### Множители сложности

```python
DIFFICULTY_MULTIPLIERS = {
    "easy": 0.8,            # Медленнее в 0.8 раза
    "medium": 1.0,          # Стандартная скорость
    "hard": 1.3,            # Быстрее в 1.3 раза
}

DIFFICULTY_LIVES = {
    "easy": 5,              # 5 жизней
    "medium": 3,            # 3 жизни
    "hard": 1,              # 1 жизнь
}
```

## Примеры использования

### Пример 1: Запуск турнира между игроками

```bash
python main.py -n "Alice" -d medium
python main.py -n "Bob" -d hard
python main.py -n "Charlie" -d easy

# Просмотр результатов
python main.py --show-stats
python main.py --show-top 5
```

### Пример 2: Сохранение и анализ прогресса

```bash
# Сыграть несколько игр
python main.py -n "Developer" -d hard

# Проверить собственную статистику
python main.py --show-player-stats "Developer"

# Очистить и начать заново
python main.py --clear-stats
```

### Пример 3: Интеграция в собственный код

```python
from main import Game
from stats_manager import StatsManager

# Создать и запустить игру
game = Game(player_name="MyPlayer", difficulty="hard", max_levels=10)
game.run()

# Проверить статистику
stats = StatsManager()
high_scores = stats.get_high_scores(5)
for i, score in enumerate(high_scores, 1):
    print(f"{i}. {score['player_name']}: {score['score']} points")
```

## Отладка и проблемы

### Pygame не установлен

```bash
pip install pygame
```

### Отсутствует директория для статистики

Директория `stats/` создается автоматически при первом запуске.

### Пустая статистика

Проверьте, что файл `stats/game_stats.json` существует и содержит данные.

## Расширение функционала

### Добавление новой сложности

1. Добавьте в `config.py`:
```python
DIFFICULTY_MULTIPLIERS["custom"] = 0.9
DIFFICULTY_LIVES["custom"] = 4
```

2. Парсер автоматически добавит новый выбор.

### Добавление новых элементов игры

1. Создайте новый класс (например, `PowerUp`)
2. Добавьте методы обработки коллизий в `Game`
3. Напишите тесты в `test_game.py`

### Сохранение дополнительных данных

Измените структуру в `StatsManager.save_game_result()`:

```python
game_record = {
    # ... существующие поля
    "custom_field": custom_value
}
```

## Лицензия

MIT License - используйте свободно в учебных и личных целях.

## Автор

Разработано как учебный проект на Python с использованием pygame.

## Контрибьютинг

Приветствуются улучшения, исправления ошибок и новые функции!

1. Сделайте fork репозитория
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменений (`git commit -m 'Add some AmazingFeature'`)
4. Push на branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## Поддержка

Если у вас есть вопросы или проблемы:
1. Проверьте секцию "Отладка и проблемы"
2. Посмотрите примеры в документации
3. Запустите тесты для проверки целостности

---

**Наслаждайтесь игрой! 🎮**
